import "./account.css"

const Account = () => {
    const renderMyStock = () => {
        let myStock = [];
        for(let i = 0; i < 10; i++) {
            myStock.push(
            )
        }

        return myStock;
    }

    return (
        <div>
            
        </div>
    );
};

export default Account;